# Module 03: Object Detection System - 아키텍처 설계서

**버전:** 1.0  
**작성일:** 2026-01-30  
**상태:** 설계 단계  
**초점:** 고정확도 객체 감지 (Accuracy-First)

---

## 📋 목차

1. [시스템 개요](#1-시스템-개요)
2. [설계 철학](#2-설계-철학)
3. [시스템 아키텍처](#3-시스템-아키텍처)
4. [핵심 컴포넌트](#4-핵심-컴포넌트)
5. [데이터 플로우](#5-데이터-플로우)
6. [인터페이스 설계](#6-인터페이스-설계)
7. [기술 스택](#7-기술-스택)
8. [성능 목표](#8-성능-목표)
9. [보안 및 안전](#9-보안-및-안전)

---

## 1. 시스템 개요

### 1.1 목적

RC 자율주행 환경에서 **실시간 객체 감지 및 분류**를 수행합니다. 트래픽 콘, 장애물, 다른 차량, 표지판 등을 높은 정확도로 감지하여 안전한 주행을 지원합니다.

### 1.2 범위

**In Scope:**
- ✅ 다중 클래스 객체 감지 (5 classes)
- ✅ 실시간 추론 (30+ FPS)
- ✅ 고정확도 감지 (mAP@0.5 > 0.90)
- ✅ 작은 객체 감지 (5cm 콘)
- ✅ Module 01, 02와 통합

**Out of Scope:**
- ❌ 3D 객체 감지 (2D만)
- ❌ 객체 추적 (Tracking) - 단순 감지만
- ❌ 의미론적 분할 (Segmentation)
- ❌ 실외 환경 (실내 RC 트랙 전용)

### 1.3 시스템 요구사항 (RC 트랙 환경 기준)

| 카테고리 | 요구사항 | 목표 값 | 비고 |
|---------|---------|---------|------|
| **정확도** | mAP@0.5 | > 0.90 | COCO metric |
| **정밀도** | Precision | > 0.92 | False Positive 최소화 |
| **재현율** | Recall | > 0.88 | 객체 누락 최소화 |
| **속도** | FPS | > 30 | RTX 5090 기준 |
| **메모리** | GPU Memory | < 4GB | 추론 시 |
| **지연시간** | Inference Latency | < 33ms | 30 FPS = 33ms/frame |

**⚠️ 중요 제약사항:**
- **환경**: 실내 RC 트랙 전용 (3×4m 공간)
- **객체 크기**: 5cm (콘) ~ 30cm (차량)
- **카메라**: Pi Camera V2 (640×480 → 640×640 crop)
- **조명**: 실내 조명 (일정한 환경)
- **정확도 우선**: 속도보다 정확도 우선

---

## 2. 설계 철학

### 2.1 핵심 원칙

1. **정확도 최우선 (Accuracy First)**
   - False Positive 최소화 (안전 주행)
   - 작은 객체 감지 능력
   - 높은 Precision & Recall
   - 모델 크기 < 속도 trade-off

2. **실시간성 보장 (Real-time Capable)**
   - 30+ FPS 유지
   - 낮은 지연시간
   - GPU 최적화

3. **강건성 (Robustness)**
   - 다양한 조명 조건
   - 가림 상황 (Occlusion)
   - 다양한 객체 배치

4. **통합성 (Integration)**
   - Module 01, 02와 원활한 연동
   - 표준 출력 형식
   - 독립 실행 가능

---

## 3. 시스템 아키텍처

### 3.1 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│              Object Detection System (Module 03)             │
└─────────────────────────────────────────────────────────────┘

┌──────────────┐
│   Camera     │
│   Input      │
│  (640×480)   │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────────────────────────────────────┐
│  Preprocessing                                               │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ - Resize to 640×640                                  │   │
│  │ - Normalize (0-1)                                    │   │
│  │ - Convert to tensor                                  │   │
│  └──────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│  YOLOv8l Backbone + Neck + Head                             │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  CSPDarknet53 Backbone                                 │ │
│  │  - Feature extraction                                  │ │
│  │  - Multi-scale features (P3, P4, P5)                  │ │
│  └────────────────┬───────────────────────────────────────┘ │
│                   │                                          │
│                   ▼                                          │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  PANet Neck                                            │ │
│  │  - Feature Pyramid Network                            │ │
│  │  - Top-down + Bottom-up paths                         │ │
│  └────────────────┬───────────────────────────────────────┘ │
│                   │                                          │
│                   ▼                                          │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  Detection Head (Anchor-free)                          │ │
│  │  - Classification                                      │ │
│  │  - Bounding Box Regression (DFL)                      │ │
│  │  - Objectness Score                                    │ │
│  └────────────────┬───────────────────────────────────────┘ │
└───────────────────┼─────────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────────┐
│  Post-processing                                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ - Non-Maximum Suppression (NMS)                      │   │
│  │ - Confidence filtering (> 0.25)                      │   │
│  │ - Class-wise NMS (IoU > 0.45)                        │   │
│  │ - Small object filtering (min_size > 10px)           │   │
│  └──────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────┐
│  Output                                                      │
│  {                                                           │
│    "boxes": [[x1, y1, x2, y2], ...],                        │
│    "classes": [0, 1, 2, ...],                               │
│    "confidences": [0.95, 0.87, ...],                        │
│    "class_names": ["cone", "obstacle", ...]                 │
│  }                                                           │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 레이어 구조

```
┌─────────────────────────────────────────┐
│  Application Layer                       │
│  - main.py                               │
│  - Integration with Module 01, 02       │
└─────────────┬───────────────────────────┘
              │
┌─────────────▼───────────────────────────┐
│  Business Logic Layer                    │
│  - ObjectDetector (Main class)          │
│  - Detection pipeline orchestration     │
└─────────────┬───────────────────────────┘
              │
┌─────────────▼───────────────────────────┐
│  Model Layer                             │
│  - YOLOv8l wrapper                       │
│  - Model loading & inference            │
│  - Output parsing                        │
└─────────────┬───────────────────────────┘
              │
┌─────────────▼───────────────────────────┐
│  Utility Layer                           │
│  - Preprocessing                         │
│  - Postprocessing (NMS)                  │
│  - Visualization                         │
│  - Metrics computation                   │
└─────────────────────────────────────────┘
```

---

## 4. 핵심 컴포넌트

### 4.1 YOLOv8l Model

**선택 이유: 정확도 우선**

| Model | Parameters | mAP@0.5 | mAP@0.5:0.95 | Speed (ms) | 선택 이유 |
|-------|-----------|---------|--------------|-----------|----------|
| YOLOv8n | 3.2M | 0.527 | 0.375 | 8.7 | ❌ 정확도 부족 |
| YOLOv8s | 11.2M | 0.629 | 0.446 | 12.4 | ❌ 여전히 낮음 |
| YOLOv8m | 25.9M | 0.676 | 0.501 | 17.3 | 🟡 괜찮으나 더 가능 |
| **YOLOv8l** | **43.7M** | **0.698** | **0.531** | **23.7** | ✅ **최적 선택** |
| YOLOv8x | 68.2M | 0.706 | 0.541 | 35.1 | ❌ 미미한 향상, 느림 |

**YOLOv8l 특징:**
- **Backbone**: CSPDarknet53 (Large)
- **Neck**: PANet (Path Aggregation Network)
- **Head**: Anchor-free + DFL (Distribution Focal Loss)
- **Input**: 640×640 (default)
- **Output**: 8400 predictions per image

**정확도 우선 설정:**
```yaml
# config/yolov8l.yaml
model: yolov8l.pt
imgsz: 640  # Higher resolution for small objects
conf: 0.25  # Confidence threshold (lower = more detections)
iou: 0.45   # NMS IoU threshold
max_det: 100  # Maximum detections per image
```

### 4.2 Dataset & Classes

**클래스 정의 (5 classes):**

```yaml
# config/dataset.yaml
names:
  0: traffic_cone     # 주황색/노란색 트래픽 콘
  1: obstacle         # 일반 장애물 (박스, 장벽)
  2: robot_car        # 다른 RC 카
  3: traffic_sign     # 미니 교통 표지판
  4: pedestrian       # 미니 피규어 (선택적)

# Class distribution strategy
distribution:
  traffic_cone: 40%   # 가장 많음 (주요 객체)
  obstacle: 30%
  robot_car: 15%
  traffic_sign: 10%
  pedestrian: 5%      # 드물게
```

**데이터셋 구성:**

| Split | Images | Annotations | 비율 |
|-------|--------|-------------|------|
| **Train** | 700 | ~3500 | 70% |
| **Val** | 150 | ~750 | 15% |
| **Test** | 150 | ~750 | 15% |
| **Total** | 1000 | ~5000 | 100% |

**수집 전략:**
1. **다양한 조명**: 밝음, 어두움, 그림자
2. **다양한 거리**: 0.5m ~ 3m
3. **다양한 각도**: 정면, 측면, 대각선
4. **가림 상황**: 부분 가림, 중첩
5. **다양한 배경**: 다른 트랙 구성

### 4.3 Data Augmentation

**목표: 데이터 5배 증강**

```python
# Training augmentation (Ultralytics built-in)
augmentation:
  # Geometric
  - degrees: 10.0          # Rotation (±10°)
  - translate: 0.1         # Translation (±10%)
  - scale: 0.5             # Scale (0.5× ~ 1.5×)
  - shear: 0.0             # No shear (objects stay upright)
  - perspective: 0.0       # No perspective (camera fixed)
  - flipud: 0.0            # No vertical flip
  - fliplr: 0.5            # Horizontal flip (50%)
  
  # Photometric
  - hsv_h: 0.015           # Hue shift
  - hsv_s: 0.7             # Saturation
  - hsv_v: 0.4             # Brightness
  - mosaic: 1.0            # Mosaic augmentation
  - mixup: 0.1             # Mixup augmentation
  
  # Noise
  - blur: 0.01             # Gaussian blur
  - noise: 0.02            # Gaussian noise
```

**결과:** 700 images → 3500 effective samples

### 4.4 Training Strategy

**정확도 최대화 전략:**

```python
# config/hyperparameters.yaml
epochs: 200               # 충분한 학습 (일반적: 100)
batch_size: 16            # 안정적 학습
imgsz: 640                # 고해상도 (작은 객체 감지)
patience: 50              # Early stopping patience

# Optimizer: AdamW
optimizer: AdamW
lr0: 0.001                # Initial learning rate
lrf: 0.01                 # Final learning rate (lr0 * lrf)
momentum: 0.937           # SGD momentum
weight_decay: 0.0005      # L2 regularization

# Loss weights (정확도 우선)
box: 7.5                  # Box loss weight
cls: 0.5                  # Class loss weight
dfl: 1.5                  # DFL loss weight

# Fine-tuning strategy
freeze: 10                # Freeze first 10 layers (backbone)
pretrained: True          # Use COCO pre-trained weights
```

**학습 단계:**

1. **Phase 1: Transfer Learning (Epochs 1-50)**
   - Freeze backbone (first 10 layers)
   - Train only head & neck
   - High learning rate (1e-3)
   
2. **Phase 2: Fine-tuning (Epochs 51-150)**
   - Unfreeze all layers
   - Lower learning rate (1e-4)
   - Full model training

3. **Phase 3: Refinement (Epochs 151-200)**
   - Very low learning rate (1e-5)
   - Fine-tune for edge cases
   - Monitor overfitting

### 4.5 Post-processing Pipeline

**정확도 보장을 위한 후처리:**

```python
# Post-processing configuration
postprocess:
  # Confidence filtering
  conf_thres: 0.25        # Confidence threshold
  
  # NMS (Non-Maximum Suppression)
  iou_thres: 0.45         # IoU threshold for NMS
  classes: null           # Filter by class (null = all)
  agnostic: False         # Class-agnostic NMS
  
  # Size filtering (작은 노이즈 제거)
  min_box_area: 100       # 최소 박스 면적 (10×10 px)
  max_box_area: 200000    # 최대 박스 면적 (거의 전체 화면)
  
  # Aspect ratio filtering (비정상 박스 제거)
  min_aspect_ratio: 0.2   # 최소 비율 (높이/폭)
  max_aspect_ratio: 5.0   # 최대 비율
  
  # Multi-scale NMS (정확도 향상)
  multi_scale: True
```

**NMS 알고리즘:**
```python
1. Sort boxes by confidence (descending)
2. For each box:
   a. If IoU with any higher-confidence box > threshold:
      Remove (suppression)
   b. Else:
      Keep (max box for this region)
3. Return kept boxes
```

---

## 5. 데이터 플로우

### 5.1 Training Pipeline

```
┌─────────────────────────────────────────────────────────────┐
│  Training Data Flow                                          │
└─────────────────────────────────────────────────────────────┘

[Raw Images] (640×480)
    │
    ▼
[Resize & Pad] → 640×640
    │
    ▼
[Augmentation]
    ├─ Mosaic (4 images)
    ├─ Mixup (2 images)
    ├─ Flip, Rotate, Scale
    └─ HSV, Blur, Noise
    │
    ▼
[Normalize] (0-1)
    │
    ▼
[Batch] (16 images)
    │
    ▼
┌──────────────────────────┐
│  YOLOv8l Model           │
│  - Forward pass          │
│  - Compute loss          │
│  - Backward pass         │
│  - Update weights        │
└──────────────────────────┘
    │
    ▼
[Loss Computation]
    ├─ Box Loss (CIoU)
    ├─ Class Loss (BCE)
    └─ DFL Loss
    │
    ▼
[Optimizer] AdamW
    │
    ▼
[Validation] (every epoch)
    │
    ▼
[Save Best Model] (highest mAP@0.5)
```

### 5.2 Inference Pipeline

```
┌─────────────────────────────────────────────────────────────┐
│  Inference Data Flow                                         │
└─────────────────────────────────────────────────────────────┘

[Input Frame] (640×480)
    │
    ▼
[Preprocessing]
    ├─ Resize: 640×640
    ├─ Normalize: [0, 1]
    └─ To Tensor: (1, 3, 640, 640)
    │
    │  < 5ms
    ▼
┌──────────────────────────┐
│  YOLOv8l Inference       │
│  - Backbone extraction   │
│  - PANet fusion          │
│  - Head prediction       │
└──────────────────────────┘
    │
    │  < 25ms (RTX 5090)
    ▼
[Raw Predictions] (8400 boxes)
    │
    ▼
[Post-processing]
    ├─ Confidence filter (> 0.25)
    ├─ NMS (IoU < 0.45)
    ├─ Size filter (> 100 px²)
    └─ Aspect ratio filter
    │
    │  < 3ms
    ▼
[Final Detections] (~10-30 boxes)
    │
    ▼
[Output Format]
{
  "boxes": [[x1, y1, x2, y2], ...],
  "classes": [0, 1, ...],
  "confidences": [0.95, 0.87, ...],
  "class_names": ["cone", "obstacle", ...]
}

Total: < 33ms (30+ FPS)
```

---

## 6. 인터페이스 설계

### 6.1 External Interface (Module 간 통합)

**Input Interface: 카메라 프레임**

```python
def detect_objects(
    frame: np.ndarray,  # (H, W, 3) RGB image
    conf_thres: float = 0.25,
    iou_thres: float = 0.45
) -> Dict:
    """
    객체 감지 수행
    
    Parameters:
        frame: Input image (H×W×3)
        conf_thres: Confidence threshold
        iou_thres: NMS IoU threshold
    
    Returns:
        {
            "boxes": List[List[float]],      # [[x1, y1, x2, y2], ...]
            "classes": List[int],            # [0, 1, 2, ...]
            "confidences": List[float],      # [0.95, 0.87, ...]
            "class_names": List[str],        # ["cone", "obstacle", ...]
            "num_detections": int,           # Total detections
            "inference_time_ms": float       # Inference time
        }
    """
```

**Output to Module 02 (LKAS):**

```python
# Obstacle avoidance integration
detections = module_03.detect_objects(frame)

# Check if obstacles in lane
lane_mask = module_01.detect_lane(frame)
obstacles_in_lane = check_overlap(detections["boxes"], lane_mask)

if obstacles_in_lane:
    lkas.emergency_brake()
    # or lkas.steer_around_obstacle(detections)
```

### 6.2 Configuration Interface

**데이터셋 설정: `config/dataset.yaml`**

```yaml
# Dataset paths
path: ./datasets
train: images/train
val: images/val
test: images/test

# Classes
nc: 5  # Number of classes
names:
  0: traffic_cone
  1: obstacle
  2: robot_car
  3: traffic_sign
  4: pedestrian

# Image settings
imgsz: 640
```

**모델 설정: `config/yolov8l.yaml`**

```yaml
# Model
model: yolov8l  # Large model for accuracy

# Hyperparameters
epochs: 200
batch: 16
imgsz: 640
patience: 50

# Optimizer
optimizer: AdamW
lr0: 0.001
lrf: 0.01
momentum: 0.937
weight_decay: 0.0005

# Loss
box: 7.5
cls: 0.5
dfl: 1.5

# Augmentation
hsv_h: 0.015
hsv_s: 0.7
hsv_v: 0.4
degrees: 10.0
translate: 0.1
scale: 0.5
fliplr: 0.5
mosaic: 1.0
mixup: 0.1
```

### 6.3 API Interface

```python
class ObjectDetector:
    """YOLOv8 기반 객체 감지기"""
    
    def __init__(
        self,
        weights: str = 'yolov8l.pt',
        device: str = 'cuda',
        conf_thres: float = 0.25,
        iou_thres: float = 0.45
    ):
        """
        Parameters:
            weights: Model weights path
            device: Device ('cuda' or 'cpu')
            conf_thres: Confidence threshold
            iou_thres: NMS IoU threshold
        """
        pass
    
    def detect(
        self,
        image: np.ndarray,
        visualize: bool = False
    ) -> Dict:
        """
        객체 감지 수행
        
        Parameters:
            image: Input image (H×W×3)
            visualize: Draw boxes on image
        
        Returns:
            Detection results
        """
        pass
    
    def detect_batch(
        self,
        images: List[np.ndarray]
    ) -> List[Dict]:
        """배치 처리"""
        pass
    
    def export_onnx(self, output_path: str):
        """ONNX 포맷으로 export"""
        pass
    
    def get_performance_stats(self) -> Dict:
        """성능 통계 반환"""
        pass
```

---

## 7. 기술 스택

### 7.1 핵심 라이브러리

| 컴포넌트 | 라이브러리 | 버전 | 용도 |
|---------|----------|------|------|
| **YOLO Framework** | Ultralytics | 8.0+ | YOLOv8 구현 |
| **Deep Learning** | PyTorch | 2.0+ | 모델 학습 & 추론 |
| **Computer Vision** | OpenCV | 4.8+ | 이미지 처리 |
| **Annotation** | CVAT / Roboflow | - | 데이터 레이블링 |
| **Augmentation** | Albumentations | 1.3+ | 데이터 증강 |
| **Metrics** | torchmetrics | 1.0+ | 평가 메트릭 |
| **Visualization** | Matplotlib | 3.7+ | 결과 시각화 |

### 7.2 개발 환경

- **Python:** 3.10+
- **OS:** Linux (Ubuntu 22.04), macOS
- **GPU:** NVIDIA RTX 5090 (24GB VRAM)
- **CUDA:** 12.1
- **cuDNN:** 8.9
- **IDE:** VS Code, PyCharm

### 7.3 의존성 관리

```txt
# requirements.txt

# Core
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.0.0

# Computer Vision
opencv-python>=4.8.0
pillow>=10.0.0

# Data
numpy>=1.24.0
pandas>=2.0.0
albumentations>=1.3.0

# Metrics
torchmetrics>=1.0.0
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Utils
tqdm>=4.65.0
pyyaml>=6.0.0
```

---

## 8. 성능 목표

### 8.1 정량적 목표 (KPI)

| 메트릭 | 목표 | 측정 방법 | 우선순위 |
|--------|------|-----------|---------|
| **mAP@0.5** | > 0.90 | COCO metric | P0 (필수) |
| **mAP@0.5:0.95** | > 0.70 | COCO metric | P0 (필수) |
| **Precision** | > 0.92 | TP / (TP + FP) | P0 (필수) |
| **Recall** | > 0.88 | TP / (TP + FN) | P1 (권장) |
| **F1-Score** | > 0.90 | 2 * P * R / (P + R) | P1 (권장) |
| **FPS** | > 30 | Frames per second | P2 (선택) |
| **Inference Time** | < 33ms | Per frame | P2 (선택) |
| **GPU Memory** | < 4GB | During inference | P2 (선택) |

**성능 목표 설정 근거:**

| 항목 | 이유 |
|------|------|
| **mAP@0.5 > 0.90** | COCO baseline (0.70) 대비 높은 목표, RC 환경은 제한적이므로 달성 가능 |
| **Precision > 0.92** | False Positive 최소화 (안전 주행 중요) |
| **Recall > 0.88** | 대부분의 객체 감지 (놓치는 것보다 과검출 선호) |
| **FPS > 30** | 실시간 주행 (30 FPS = 인간 시각 시스템) |

### 8.2 클래스별 성능 목표

| Class | mAP@0.5 | Precision | Recall | 난이도 |
|-------|---------|-----------|--------|--------|
| **traffic_cone** | > 0.92 | > 0.94 | > 0.90 | 보통 (작은 객체) |
| **obstacle** | > 0.90 | > 0.92 | > 0.88 | 쉬움 (큰 객체) |
| **robot_car** | > 0.88 | > 0.90 | > 0.86 | 어려움 (다양한 모양) |
| **traffic_sign** | > 0.85 | > 0.88 | > 0.84 | 어려움 (작고 다양) |
| **pedestrian** | > 0.80 | > 0.85 | > 0.80 | 매우 어려움 (매우 작음) |

### 8.3 정성적 목표

- ✅ **작은 객체 감지**: 5cm 콘을 3m 거리에서 감지
- ✅ **조명 강건성**: 밝음/어두움/그림자에서 일관된 성능
- ✅ **가림 처리**: 50% 가림까지 감지
- ✅ **부드러운 감지**: 연속 프레임에서 안정적 박스
- ✅ **낮은 오검출**: 배경을 객체로 오인 최소화

---

## 9. 보안 및 안전

### 9.1 안전 메커니즘

**Fail-Safe 설계:**

```python
1. Input Validation
   - 이미지 크기 검증 (640×640)
   - 채널 수 검증 (3 channels)
   - 값 범위 검증 (0-255 or 0-1)

2. Confidence Filtering
   - 낮은 신뢰도 (<0.25) 제거
   - 매우 작은 박스 제거 (<100 px²)
   - 비정상 비율 박스 제거

3. Graceful Degradation
   - 모델 로딩 실패 시: 빈 결과 반환
   - GPU 메모리 부족 시: CPU fallback
   - 추론 실패 시: 이전 결과 재사용

4. Performance Monitoring
   - FPS 모니터링 (< 15 FPS이면 경고)
   - GPU 메모리 모니터링
   - 지연시간 tracking
```

### 9.2 테스트 전략

**단위 테스트 (Unit Test):**
- Preprocessing 함수
- Post-processing (NMS)
- 각 클래스 감지
- Edge case 커버리지 > 90%

**통합 테스트 (Integration Test):**
- 전체 파이프라인 E2E
- Module 01, 02와 통합
- 실시간 성능 테스트

**성능 테스트 (Performance Test):**
- FPS 벤치마크
- 지연시간 프로파일링
- GPU 메모리 사용량
- 장시간 안정성 (1시간 연속)

**정확도 테스트 (Accuracy Test):**
- Test set mAP 측정
- 클래스별 성능
- 작은 객체 감지 능력
- 가림 상황 처리

### 9.3 로깅 및 모니터링

**로그 레벨:**
```python
DEBUG:   세부 추적 정보
INFO:    일반 감지 결과
WARNING: 낮은 FPS, 높은 지연시간
ERROR:   추론 실패
CRITICAL: 시스템 중단
```

**모니터링 대시보드:**
- 실시간 감지 결과 (박스)
- FPS & 지연시간 그래프
- 클래스별 감지 빈도
- GPU 메모리 사용량
- Confidence 분포

---

## 10. 데이터 수집 전략

### 10.1 수집 계획

**목표: 1,000 images (5,000+ annotations)**

**수집 환경:**
```
1. RC 트랙 A (주 트랙)
   - 크기: 3m × 4m
   - 조명: 천장 LED
   - 수집: 600 images

2. RC 트랙 B (보조 트랙)
   - 크기: 2.5m × 3.5m
   - 조명: 자연광 + LED
   - 수집: 400 images
```

**수집 조건:**
```python
조명:
  - 밝음 (1000 lux): 30%
  - 보통 (500 lux): 40%
  - 어두움 (200 lux): 20%
  - 그림자: 10%

거리:
  - 가까움 (0.5-1m): 30%
  - 중간 (1-2m): 40%
  - 멀리 (2-3m): 30%

각도:
  - 정면 (0-30°): 40%
  - 측면 (30-60°): 35%
  - 대각 (60-90°): 25%

객체 수:
  - 1-2개: 20%
  - 3-5개: 50%
  - 6-10개: 25%
  - 10개 이상: 5%
```

### 10.2 레이블링 전략

**도구: CVAT (Computer Vision Annotation Tool)**

**레이블링 가이드라인:**
```
1. Bounding Box
   - 객체 전체를 포함 (margin 약간)
   - 가림된 부분도 추정하여 포함
   - 최소 크기: 10×10 px

2. 클래스 할당
   - traffic_cone: 주황/노랑 콘
   - obstacle: 박스, 장벽, 기타
   - robot_car: RC 카 전체
   - traffic_sign: 표지판 (전체 포함)
   - pedestrian: 피규어 (머리부터 발까지)

3. 품질 관리
   - 각 이미지 2명이 검토 (inter-annotator agreement)
   - 애매한 경우: 포함 (recall 우선)
   - 부분 가림 (>50%): 레이블링
   - 부분 가림 (<20%): 제외
```

### 10.3 데이터 검증

**품질 체크리스트:**
- [ ] 모든 박스가 객체 포함
- [ ] 클래스 올바르게 할당
- [ ] 중복 박스 없음
- [ ] 너무 작은 박스 (<10px) 제거
- [ ] 이미지 해상도 일관성
- [ ] Train/Val/Test 분리 확인

---

## 11. 마일스톤 및 일정

### Phase 1: 설계 & 준비 (Week 1, 2일)
- [x] 아키텍처 설계서
- [ ] 구현 명세서
- [ ] 검증서
- [ ] 데이터 수집 계획

### Phase 2: 데이터 수집 (Week 1-2, 5일)
- [ ] 이미지 수집 (1000장)
- [ ] CVAT 레이블링
- [ ] 품질 검증
- [ ] Train/Val/Test split

### Phase 3: 구현 (Week 2, 3일)
- [ ] YOLOv8 통합
- [ ] Dataset loader
- [ ] Training pipeline
- [ ] Inference pipeline
- [ ] Post-processing

### Phase 4: 학습 (Week 2-3, 4일)
- [ ] Baseline training (100 epochs)
- [ ] Hyperparameter tuning
- [ ] Extended training (200 epochs)
- [ ] Model selection

### Phase 5: 평가 & 문서화 (Week 3-4, 2일)
- [ ] Test set 평가
- [ ] Ablation studies
- [ ] Performance documentation
- [ ] Integration testing

**총 예상 기간:** 2-3주

---

## 12. 참고 자료

### 학술 논문
1. **YOLOv8** (Ultralytics, 2023) - Official documentation
2. **YOLOv5** (Jocher et al., 2020) - Architecture foundation
3. **Focal Loss** (Lin et al., 2017) - Loss function for hard examples

### 기술 문서
- [Ultralytics YOLOv8 Docs](https://docs.ultralytics.com/)
- [YOLO GitHub](https://github.com/ultralytics/ultralytics)
- [CVAT Annotation Guide](https://opencv.github.io/cvat/)

### 오픈소스 참고
- [Roboflow Universe](https://universe.roboflow.com/)
- [COCO Dataset](https://cocodataset.org/)

---

**문서 버전 이력:**
- v1.0 (2026-01-30): 초안 작성 (정확도 우선 전략)

**리뷰어:** [TBD]  
**승인자:** [TBD]

**다음 단계:** 구현 명세서 작성
